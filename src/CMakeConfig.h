#define Lazarus_VERSION_MAJOR 1
#define Lazarus_VERSION_MINOR 0

#define Lazarus_Install_Path ""

#define USETOMCRYPT

//indicates if ZLIB support is available, values are either "USEZLIB" or "NOZLIB" 
#define USEZLIB

//indicates if CPU power support is available, values are either "USECPUPOWER" or "NOCPUPOWER" 
#define USECPUPOWER

//indicates if lmsensors support is available, values are either "USEHWSENSORS" or "NOHWSENSORS" 
#define USELMSENSORS

//indicates if CURL support is available, values are either "USECURL" or "NOCURL" 
#define USECURL
